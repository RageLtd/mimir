# Mimir server
MIMIR_PORT=8080
MIMIR_HOST=0.0.0.0

# vLLM on the Spark
VLLM_BASE_URL=
VLLM_MODEL=

# LM Studio — OpenAI-compatible local server (opt-in). Default port 1234.
# Set this to enable LM Studio model discovery. Models exposed are whatever
# is currently loaded in the LM Studio UI at startup; restart mimir-server
# after loading new models to pick them up.
# In LM Studio: enable "Serve on Local Network" if accessing from a different host.
LMSTUDIO_BASE_URL=

# SurrealDB (docker service name)
SURREAL_URL=http://surrealdb:8000/rpc
SURREAL_NS=mimir
SURREAL_DB=mimir
SURREAL_USER=
SURREAL_PASS=

SMALL_MODEL_BASE_URL=
SMALL_MODEL_PROVIDER_TYPE=ollama
SMALL_MODEL_MODEL=qwen3.5:9b

EMBED_MODEL=qwen3-embedding:0.6b
EMBED_TYPE=ollama
EMBED_BASE_URL=

# Context7 — docs lookup (free tier works without key, key raises limits)
# Get one at https://context7.com/dashboard
CONTEXT7_API_KEY=

# Tavily — web search (required for web_search tool)
# Get one at https://app.tavily.com
TAVILY_API_KEY=

# OpenCode Zen — multi-provider gateway for frontier model escalation
# Get your key at https://opencode.ai/auth
# Leave empty to disable Zen routing (all requests go to local vLLM)
# Enable OpenCode Zen Go subscription models via ZEN_GO_ENABLED
OPENCODE_API_KEY=
ZEN_GO_ENABLED=true
CHUTES_API_KEY=

# OpenRouter — multi-provider gateway (OpenAI-compatible)
# Get your key at https://openrouter.ai/keys
# Leave empty to disable OpenRouter routing
OPENROUTER_API_KEY=
OPENROUTER_ZDR=true
OPENROUTER_FREE=true
OPENROUTER_EXCLUDE=

# Ollama Cloud — hosted ollama.com gateway (OpenAI-compatible).
# Get your key at https://ollama.com/settings/keys
# Setting this key auto-registers the `ollama-cloud` provider and its catalog
# from models.dev — no other config needed. Distinct from the LOCAL Ollama
# instance (SMALL_MODEL_*/EMBED_*/OLLAMA_BASE_URL); the two coexist, but if you
# run both, overlapping model ids (e.g. gpt-oss:120b) resolve to cloud unless
# you qualify them with a provider prefix (ollama/… vs ollama-cloud/…).
OLLAMA_API_KEY=

# System prompt
SYSTEM_PROMPT_PATH=./system-prompt.md

# Memory hygiene relocated client-side (MIM-86) — the sweep runs locally
# over the replica via the plugin's `hygiene` command on the developer's
# extraction model (MIMIR_EXTRACTION_* trio). No server-side knobs remain.

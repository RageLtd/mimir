---
globs: ["*.ts", "*.tsx", "*.js", "*.jsx", "*.mts", "*.mjs", "package.json", "bunfig.toml"]
---
# Bun over Node

Use Bun as runtime and package manager for TypeScript/JavaScript projects. If the project already uses npm or pnpm (has `package-lock.json` or `pnpm-lock.yaml`), continue with the existing package manager.

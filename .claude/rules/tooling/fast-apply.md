# Morph Fast Apply

When the Morph MCP server is available, use `edit_file` over the built-in `Edit` tool or full file writes. It works with partial code snippets — no need for full file content.

If Morph MCP is not connected, use the built-in `Edit` tool.
